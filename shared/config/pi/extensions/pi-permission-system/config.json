{
  "permission": {
    "*": "allow",
    "path": {
      "*": "allow",
      "*.env": "deny",
      "*.env.*": "deny",
      "*.env.example": "allow",
      "*.env.template": "allow"
    },
    "bash": {
      "*": "allow",
      "rm -rf *": "deny",
      "sudo *": "ask",
      "timeout *": { "action": "deny", "reason": "Do not wrap commands in the shell 'timeout' binary; it is an indirection wrapper that pauses agents. Bound long-running commands with bg_run and timeoutSeconds so the command runs plainly within the permission rules." },
      "find /": { "action": "deny", "reason": "Unscoped find from filesystem root stalls; scope the search to a specific directory" },
      "find / *": { "action": "deny", "reason": "Unscoped find from filesystem root stalls; scope the search to a specific directory" },
      "find ~": { "action": "deny", "reason": "Unscoped find over the whole home directory stalls; scope the search to a specific directory" },
      "find ~ *": { "action": "deny", "reason": "Unscoped find over the whole home directory stalls; scope the search to a specific directory" },
      "find $HOME": { "action": "deny", "reason": "Unscoped find over the whole home directory stalls; scope the search to a specific directory" },
      "find $HOME *": { "action": "deny", "reason": "Unscoped find over the whole home directory stalls; scope the search to a specific directory" },
      "find /Users/jacob": { "action": "deny", "reason": "Unscoped find over the whole home directory stalls; scope the search to a specific directory" },
      "find /Users/jacob *": { "action": "deny", "reason": "Unscoped find over the whole home directory stalls; scope the search to a specific directory" }
    },
    "find": {
      "*": "allow",
      "/": { "action": "deny", "reason": "Unscoped find from filesystem root stalls; scope the search to a specific directory" },
      "~": { "action": "deny", "reason": "Unscoped find over the whole home directory stalls; scope the search to a specific directory" },
      "$HOME": { "action": "deny", "reason": "Unscoped find over the whole home directory stalls; scope the search to a specific directory" },
      "/Users/jacob": { "action": "deny", "reason": "Unscoped find over the whole home directory stalls; scope the search to a specific directory" }
    },
    "external_directory": {
      "*": "deny",
      "/tmp/*": "allow",
      "/private/tmp/*": "allow",
      "/var/folders/*": "allow",
      "/private/var/folders/*": "allow",
      "~/.agents/*": "allow",
      "~/.pi/*": "allow",
      "~/.config/opencode/*": "allow",
      "~/.config/agents/*": "allow",
      "~/.config/cmux/*": "allow",
      "~/Documents/GitHub/*": "allow",
      "~/Documents/orca/*": "allow",
      "~/Downloads/*": "ask",
      "~/Library/Application Support/premind/event-details/*": "allow"
    }
  }
}
